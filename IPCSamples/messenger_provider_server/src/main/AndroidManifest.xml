<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="leavesc.hello.messenger_server">



    <!-- 1. 先声明权限 -->
    <permission android:name="com.itheima.permission.mybroadcast"></permission>
    <!-- 2. 再使用 -->
    <uses-permission android:name="com.itheima.permission.mybroadcast"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">



        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MessengerService"
            android:enabled="true"
            android:exported="true" />


        <!--
       // 声明外界进程可访问该Provider的权限（读 & 写）
       android:permission="scut.carson_ho.PROVIDER"

       // 权限可细分为读 & 写的权限
       // 外界需要声明同样的读 & 写的权限才可进行相应操作，否则会报错
       // android:readPermisson = "scut.carson_ho.Read"
       // android:writePermisson = "scut.carson_ho.Write"

       确保唯一，手机上面不同的app不能有相同的provider，
       android:authorities="leavesc.hello.messenger_server.MyProvider"
       -->
        <provider
            android:name="leavesc.hello.messenger_server.MyProvider"
            android:authorities="leavesc.hello.messenger_server.MyProvider"
            android:exported="true"
            android:permission="scut.carson_ho.permission.PROVIDER"
            >
        </provider>
        <!-- android:permission="scut.carson_ho.permission.PROVIDER"-->

        <provider
            android:name="leavesc.hello.messenger_server.ProviderStudy"
            android:authorities="leavesc.hello.messenger_server.ProviderStudy"
            android:exported="true"
            android:permission="scut.carson_ho.permission.PROVIDER" >
        </provider>

        <activity
            android:screenOrientation="portrait"
            android:name="leavesc.hello.messenger_server.PermissionActivity"
            android:exported="true"
            android:permission="leavesc.hello.messenger_server.permission.PermissionActivity"
            >
            <intent-filter>
                <action android:name="leavesc.hello.messenger_server.PermissionActivity"/>
                <category android:name="android.intent.category.DEFAULT"></category>
            </intent-filter>
        </activity>


        <receiver
            android:exported="true"
            android:name="leavesc.hello.messenger_server.MyBroadcastReceiver">

            <intent-filter >
                <action android:name="com.itheima.action.mybroadcast"/>
            </intent-filter>
        </receiver>


    </application>

</manifest>