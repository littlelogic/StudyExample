<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="leavesc.hello.messenger_client">

    <!--声明本应用 可允许通信的权限-->
    <!--<permission android:name="scut.carson_ho.PROVIDER" android:protectionLevel="normal"/>-->
    <!--细分读 & 写权限如下，但本Demo直接采用全权限-->
    <!--<permission android:name="scut.carson_ho.Write" android:protectionLevel="normal"/>-->
    <!--<permission android:name="scut.carson_ho.PROVIDER" android:protectionLevel="normal"/>-->


    <!-- 自定义权限，声明权限 -->
    <permission android:name="scut.carson_ho.permission.PROVIDER"></permission>
    <!-- 使用权限 -->
    <uses-permission android:name="scut.carson_ho.permission.PROVIDER"/>


    <permission android:name="leavesc.hello.messenger_server.permission.PermissionActivity"></permission>
    <!-- 使用权限 -->
    <uses-permission android:name="leavesc.hello.messenger_server.permission.PermissionActivity"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>